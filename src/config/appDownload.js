// URL pública de Evalúa Fácil en la web.
//
// El QR general de la plataforma apunta aquí: al escanearlo el usuario llega
// al sitio oficial y desde ahí puede entrar como docente o como estudiante.
// La app nativa está en construcción; cuando esté disponible se actualizará
// este archivo para reflejar el nuevo destino.
export const APP_WEB_URL = 'https://www.evaluafacil.mx/'

// Alias para no romper imports existentes en archivos que aún no fueron migrados.
export const APP_DOWNLOAD_URL = APP_WEB_URL

// Siempre true: la web siempre existe.
export const APP_DOWNLOAD_READY = true
