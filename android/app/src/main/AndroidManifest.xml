<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Notificaciones push/locales (Android 13+ las requiere en runtime también,
         pero el permiso debe declararse aquí para poder pedirlo). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Sin esto, @capacitor/local-notifications cae a alarmas INEXACTAS en
         Android 12+ (AlarmManager.canScheduleExactAlarms() siempre false):
         el aviso de "clase por comenzar" puede retrasarse mucho o nunca
         entregarse bajo Doze/ahorro de batería. Confirmado en depuración con
         dispositivo real — sin este permiso, un aviso a 5 min antes de una
         clase no se disparó ni siquiera 30 min después. En Android 13+ el
         docente aún debe conceder el acceso especial "Alarmas y
         recordatorios" (ver checkExactNotificationSetting/
         changeExactNotificationSetting en utils/localReminders.js). -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
</manifest>
